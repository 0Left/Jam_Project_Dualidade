using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HeadRec : MonoBehaviour
{
    /*
    private void OnTriggerEnter2D(Collider2D other) {
        if(other.CompareTag("Box")){
            gameObject.transform.parent.transform.gameObject.GetComponent<MainChar_Mov>().Die();
        }
    }
    */
}
